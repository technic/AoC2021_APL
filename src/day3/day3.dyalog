a ← ⊃⎕NGET'day3/input.txt'1

b ← (+/ ⍉↑ '1'=a) > (⍴a) ÷ 2
(2 ⊥ b) × (2 ⊥ (b=0))

aa ← ↓⍉⌽( ((⍴a)⍴1), (↑('1'=a)) )
n ← {cnt ← (+/⍵) ⋄ cnt > 1 : (( (+/ (⍺×⍵) × 2) ≥ cnt) = ⍺) × ⍵ ⋄ ⍵}/ aa
m ← {cnt ← (+/⍵) ⋄ cnt > 1 : (( (+/ (⍺×⍵) × 2) < cnt) = ⍺) × ⍵ ⋄ ⍵}/ aa
(2⊥⊃(⊃n)/a='1')×(2⊥⊃(⊃m)/a='1')
